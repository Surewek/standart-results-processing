export function getResultTable(array) {
    return `res`
}

export function drawBarChart(values) {
    return ``
}

export function drawBar(index) {
    return `<div class="bar bar-${index}"></div>`;
}